require('nvim-tree').setup {
    diagnostics = {
        enable = true,
        debounce_delay = 0
    },
    git = {
        enable = true,
        ignore = false
    },
    view = {
        side = "right",
    }
}
